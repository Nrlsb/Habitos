name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # cada 10 minutos

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping server
        run: curl -sf ${{ secrets.RENDER_URL }}/ping
